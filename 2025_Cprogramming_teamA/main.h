/*
 파일명: main.h
 작성자: 
 설명: 프로젝트 공통 헤더 파일로 전역 상수, 변수 및 함수 원형 선언함
 */

#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include "book.h" // Book 구조체 정의를 참조하기 위해 포함함

 // ---------------------------------------------------------
 // 프로젝트 전역 상수 정의함
 // ---------------------------------------------------------
#define MAX_BOOKS 1000 // 최대 도서 저장 가능 개수 설정함
#define MAX_USERS 100  // 최대 사용자 저장 가능 개수 설정함

// ---------------------------------------------------------
// 전역 변수 외부 참조 선언함 (extern)
// 실제 메모리 할당은 main.c에서 수행하고 여기서는 참조만 함
// ---------------------------------------------------------
extern Book g_books[MAX_BOOKS]; // 도서 데이터 저장 배열 외부 참조함
extern int g_book_count;        // 현재 저장된 도서 개수 변수 외부 참조함

// ---------------------------------------------------------
// 함수 원형 선언함 (Book I/O 모듈)
// ---------------------------------------------------------
void loadBooks();   // 파일에서 도서 데이터 불러오는 함수 선언함
void saveBooks();   // 파일에 도서 데이터 저장하는 함수 선언함
void manageBooks(); // 관리자 도서 관리 메뉴 진입 함수 선언함

// ---------------------------------------------------------
// 테스트 및 추후 구현 예정 함수 선언함
// ---------------------------------------------------------
void searchBook();  // 사용자 도서 검색 함수 선언함 (추후 구현 예정임)

#endif